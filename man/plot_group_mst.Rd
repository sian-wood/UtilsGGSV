% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_cluster_mst.R
\name{plot_group_mst}
\alias{plot_group_mst}
\alias{plot_cluster_mst}
\title{Plot minimum-spanning tree of groups with per-variable node colouring}
\usage{
plot_group_mst(
  .data,
  group,
  vars = NULL,
  layout_algorithm = c("kamada-kawai", "mds"),
  coord_equal = TRUE,
  suppress_axes = NULL,
  col_clusters = NULL,
  node_fill_by = "variable",
  palette_group = "auto",
  palette = "bipolar",
  col = c("#2166AC", "#F7F7F7", "#B2182B"),
  col_positions = "auto",
  white_range = c(0.4, 0.6),
  na_rm = TRUE,
  n_col = NULL,
  n_row = NULL,
  label_x = 0,
  label_y = 1,
  hjust = -0.5,
  vjust = 1.5,
  font_size = 14,
  thm = cowplot::theme_cowplot(font_size = font_size) + ggplot2::theme(plot.background =
    ggplot2::element_rect(fill = "white", colour = NA), panel.background =
    ggplot2::element_rect(fill = "white", colour = NA)),
  grid = cowplot::background_grid(major = "xy")
)

plot_cluster_mst(
  .data,
  cluster,
  palette_cluster = "auto",
  palette = "bipolar",
  ...
)
}
\arguments{
\item{.data}{data.frame. Rows are observations. Must contain a column
identifying group membership and columns for variable values.}

\item{group}{character. Name of the column in \code{.data} that identifies
group membership.}

\item{vars}{character vector or \code{NULL}. Names of columns in \code{.data} to
use as variables. If \code{NULL}, all columns except \code{group} are used.
Default is \code{NULL}.}

\item{layout_algorithm}{character. Layout algorithm for positioning nodes.
One of \code{"kamada-kawai"} (default) or \code{"mds"}. \code{"kamada-kawai"} uses the
Kamada-Kawai force-directed algorithm on the MST graph via
\code{igraph::layout_with_kk}, matching the FlowSOM visualisation style.
\code{"mds"} uses classical multidimensional scaling of the full distance
matrix via \code{stats::cmdscale}.}

\item{coord_equal}{logical. Whether to enforce equal visual scaling on both
axes (one unit on the x-axis equals one unit on the y-axis) via
\code{ggplot2::coord_equal()}. Default is \code{TRUE}.}

\item{suppress_axes}{logical or \code{NULL}. Whether to suppress axis text,
ticks, lines, and titles. When \code{NULL} (default), the value is inherited
from \code{coord_equal} — axes are suppressed when equal scaling is active.}

\item{col_clusters}{named character vector or \code{NULL}. Per-cluster colours
applied to node borders and text labels. Names should match cluster
labels. When \code{NULL} (default), colours are chosen automatically by number
of groups: Okabe-Ito for up to 8, ColorBrewer Paired for up to 12,
Kelly's palette (requires \code{Polychrome}) for up to 21, Glasbey's palette
(requires \code{Polychrome}) for up to 31, and \code{hue_pal()} for larger
numbers.}

\item{node_fill_by}{character. Controls what the node fill encodes. One of
\code{"variable"} (default) or \code{"cluster"}. See the \strong{Node fill} section of
Details.}

\item{palette_group}{character. Palette used for automatic colour assignment
when \code{col_clusters} is \code{NULL}. One of \code{"auto"} (default), \code{"okabe_ito"},
\code{"paired"}, \code{"kelly"}, \code{"glasbey"}, or \code{"hue_pal"}. See the \strong{Colour
palette} section of Details.}

\item{palette}{character or \code{NULL}. Named colour palette for the continuous
node fill scale. Forwarded to \code{\link[=plot_group_mst]{plot_group_mst()}}. Default is \code{"bipolar"}.}

\item{col}{character vector. Colours used to fill nodes, ordered from low
to high values. Default is \code{c("#2166AC", "#F7F7F7", "#B2182B")} (blue,
white, red). Any number of colours (>= 2) is accepted. Ignored when
\code{palette} is not \code{NULL}.}

\item{col_positions}{numeric vector or \code{"auto"}. Positions (in [0, 1]) at
which each colour in \code{col} is placed on the fill scale. Must be the same
length as \code{col}, sorted in ascending order, with the first value \code{0} and
the last value \code{1}. When \code{"auto"} (default) and \code{col} has exactly three
colours, the middle colour is stretched over \code{white_range}. In all other
\code{"auto"} cases the colours are evenly spaced from 0 to 1. Ignored when
\code{palette} is not \code{NULL}.}

\item{white_range}{numeric vector of length 2. The range of positions (on a
0-1 scale) over which the middle colour is stretched. Only used when \code{col}
has exactly three colours and \code{col_positions = "auto"}. Also applied to
diverging \code{palette} presets. Default is \code{c(0.4, 0.6)}.}

\item{na_rm}{logical. Whether to remove \code{NA} values before computing
per-cluster medians and ECDF percentiles. When \code{TRUE} (default), \code{NA}
values are removed and a message is issued showing how many were removed
per variable. When \code{FALSE}, \code{NA} values are passed through: node fill
values will be \code{NA} (rendered as grey by default) where a variable has no
non-missing observations in a cluster.}

\item{n_col}{integer or \code{NULL}. Number of columns passed to
\code{cowplot::plot_grid}. If supplied (or if \code{n_row} is supplied) a single
combined figure is returned instead of a list. Default is \code{NULL}.}

\item{n_row}{integer or \code{NULL}. Number of rows passed to
\code{cowplot::plot_grid}. If supplied (or if \code{n_col} is supplied) a single
combined figure is returned instead of a list. Default is \code{NULL}.}

\item{label_x}{numeric. x position of the plot labels within each panel
in grid mode. Passed to \code{cowplot::plot_grid}. Default is \code{0}.}

\item{label_y}{numeric. y position of the plot labels within each panel
in grid mode. Passed to \code{cowplot::plot_grid}. Default is \code{1}.}

\item{hjust}{numeric. Horizontal justification of the plot labels in grid
mode. Passed to \code{cowplot::plot_grid}. Default is \code{-0.5}.}

\item{vjust}{numeric. Vertical justification of the plot labels in grid
mode. Passed to \code{cowplot::plot_grid}. Default is \code{1.5}.}

\item{font_size}{numeric. Font size passed to \code{cowplot::theme_cowplot}.
Default is \code{14}.}

\item{thm}{ggplot2 theme object or \code{NULL}. Default is
\code{cowplot::theme_cowplot(font_size = font_size)} with a white plot
background. Set to \code{NULL} to apply no theme adjustment.}

\item{grid}{ggplot2 panel grid or \code{NULL}. Default is
\code{cowplot::background_grid(major = "xy")}. Set to \code{NULL} for no grid.}

\item{cluster}{character. Name of the column in \code{.data} that identifies
group membership. Alias for the \code{group} parameter.}

\item{palette_cluster}{character. Alias for \code{palette_group} in
\code{\link[=plot_group_mst]{plot_group_mst()}}. See the \strong{Colour palette} section of Details.}

\item{...}{Additional arguments passed to \code{\link[=plot_group_mst]{plot_group_mst()}}.}
}
\value{
A named list of ggplot2 objects (one per variable) when neither
\code{n_col} nor \code{n_row} is specified. A \code{cowplot::plot_grid} figure when
\code{n_col} or \code{n_row} is specified.
}
\description{
Computes the minimum-spanning tree (MST) over groups, where the distance
between two groups is the Euclidean distance between their median variable
profiles. The MST is built from the full pairwise Euclidean distance matrix
(i.e. a fully connected undirected weighted graph), matching the approach
used by FlowSOM (\code{BuildMST}). The node layout is determined once and shared
across all per-variable plots.

Two layout algorithms are supported via \code{layout_algorithm}:
\itemize{
\item \code{"kamada-kawai"} (default): uses the Kamada-Kawai force-directed algorithm
(\code{igraph::layout_with_kk}) on the MST graph, matching the FlowSOM
visualisation style.
\item \code{"mds"}: uses classical multidimensional scaling (\code{stats::cmdscale}) of
the full Euclidean distance matrix.
}

For each variable, a separate plot is produced in which each group node
is \strong{filled} according to the ECDF-standardised percentile of that
group's median value for the variable — the same scaling used by
\code{\link[=plot_group_heatmap]{plot_group_heatmap()}}. The node border and label colour encode group
identity and can be overridden via \code{col_clusters}.

By default the function returns a \strong{named list of ggplot2 objects}, one
per variable. If \code{n_col} or \code{n_row} is supplied the plots are combined into
a single figure using \code{cowplot::plot_grid}, with variable names as labels.
\subsection{Colour palette}{

When \code{col_clusters} is \code{NULL}, group colours are assigned automatically
based on \code{palette_group}. The \code{"auto"} strategy selects a palette by the
number of groups:
\itemize{
\item \strong{1–8 groups}: Okabe-Ito — colorblind-safe 8-colour palette.
\item \strong{9–12 groups}: ColorBrewer Paired — 12 colours pairing light and dark
versions of 6 hues.
\item \strong{13–21 groups}: Kelly's palette (optional \code{Polychrome} package) — 21
colours of maximum perceptual contrast (white excluded). Falls back to
\code{hue_pal()} with a warning if \code{Polychrome} is not installed.
\item \strong{22–31 groups}: Glasbey's palette (optional \code{Polychrome} package) — 31
algorithmically spaced colours (white excluded). Falls back to \code{hue_pal()}
with a warning if \code{Polychrome} is not installed.
\item \strong{> 31 groups}: \code{hue_pal()} — evenly spaced hues (a warning is issued).
}

Set \code{palette_group} explicitly to override the automatic selection (provided
the chosen palette supports at least as many colours as there are groups).
}

\subsection{Node fill}{

By default (\code{node_fill_by = "variable"}) each variable produces a separate
plot in which node fill encodes the ECDF-standardised percentile of that
cluster's median — a continuous gradient from low (blue) to high (red) using
the palette controlled by \code{palette} / \code{col} / \code{col_positions}.

Set \code{node_fill_by = "cluster"} to instead fill nodes by cluster identity
using the same discrete palette chosen by \code{palette_group} / \code{col_clusters}.
In this mode the function returns a \strong{single ggplot2 object} (not a list)
because the fill is the same regardless of variable.
}
}
\examples{
set.seed(1)
.data <- data.frame(
  group = rep(paste0("C", 1:3), each = 20),
  var1 = c(rnorm(20, 2), rnorm(20, 0), rnorm(20, -2)),
  var2 = c(rnorm(20, -1), rnorm(20, 1), rnorm(20, 0))
)
# Default: Kamada-Kawai layout, returns a named list of plots
plot_list <- plot_group_mst(.data, group = "group")

# MDS layout
plot_group_mst(.data, group = "group", layout_algorithm = "mds")

# Combined grid with 2 columns
plot_group_mst(.data, group = "group", n_col = 2)
}
