% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_cluster_heatmap.R
\name{plot_group_heatmap}
\alias{plot_group_heatmap}
\alias{plot_cluster_heatmap}
\title{Plot heat map of scaled variable values per group}
\usage{
plot_group_heatmap(
  .data,
  group,
  vars = NULL,
  scale_method = "ecdf",
  palette = "bipolar",
  col = c("#2166AC", "#F7F7F7", "#B2182B"),
  col_positions = "auto",
  white_range = c(0.4, 0.6),
  na_rm = TRUE,
  font_size = 14,
  thm = cowplot::theme_cowplot(font_size = font_size) + ggplot2::theme(plot.background =
    ggplot2::element_rect(fill = "white", colour = NA), panel.background =
    ggplot2::element_rect(fill = "white", colour = NA)),
  grid = cowplot::background_grid(major = "xy"),
  show_values = FALSE,
  values_format = NULL,
  values_col = "black",
  values_size = 3
)

plot_cluster_heatmap(.data, cluster, ...)
}
\arguments{
\item{.data}{data.frame. Rows are observations. Must contain a column
identifying group membership and columns for variable values.}

\item{group}{character. Name of the column in \code{.data} that identifies
group membership.}

\item{vars}{character vector or \code{NULL}. Names of columns in \code{.data} to
use as variables. If \code{NULL}, all columns except \code{group} are used.
Default is \code{NULL}.}

\item{scale_method}{character. Method used to scale variable values for
colouring cells. One of \code{"ecdf"} (default), \code{"zscore"}, \code{"raw"},
\code{"minmax"}, or \code{"minmax_var"}.
\itemize{
\item \code{"ecdf"}: Each cell shows the percentile of the cluster's median value
compared to all observations outside the cluster (empirical CDF).
Fill values are in [0, 1] and the legend uses percent labels.
\item \code{"zscore"}: Each cell shows the z-score of the cluster's median
relative to all observations of that variable
(\code{(median - mean) / sd}). Fill values are unbounded.
\item \code{"raw"}: Each cell shows the raw median value. Fill values are
unbounded.
\item \code{"minmax"}: Each cell shows the cluster median scaled to [0, 1]
using the global minimum and maximum across all observations of all
variables. Fill values are in [0, 1] and the legend uses percent
labels.
\item \code{"minmax_var"}: Each cell shows the cluster median scaled to [0, 1]
using the minimum and maximum of all observations within each variable
separately. Fill values are in [0, 1] and the legend uses percent
labels.
}}

\item{palette}{character or \code{NULL}. Named colour palette for the continuous
fill scale. When not \code{NULL}, overrides \code{col} and \code{col_positions}. Available
palettes: \code{"bipolar"} (default, blue-white-red), \code{"alarm"}
(green-white-red, good-to-bad), \code{"accessible"} (blue-white-orange,
colour-blind-safe diverging), \code{"heat"} (light-yellow to dark-red,
sequential), \code{"sky"} (white to navy, sequential). Set to \code{NULL} to use
\code{col} and \code{col_positions} directly.}

\item{col}{character vector. Colours used to fill tiles, ordered from low
to high values. Default is \code{c("#2166AC", "#F7F7F7", "#B2182B")} (blue,
white, red). Any number of colours (>= 2) is accepted. Ignored when
\code{palette} is not \code{NULL}.}

\item{col_positions}{numeric vector or \code{"auto"}. Positions (in [0, 1]) at
which each colour in \code{col} is placed on the fill scale. Must be the same
length as \code{col}, sorted in ascending order, with the first value \code{0} and
the last value \code{1}. When \code{"auto"} (default) and \code{col} has exactly three
colours and \code{scale_method = "ecdf"}, the middle colour is stretched over
\code{white_range} (the current default behaviour). In all other \code{"auto"} cases
the colours are evenly spaced from 0 to 1. Ignored when \code{palette} is not
\code{NULL}.}

\item{white_range}{numeric vector of length 2. The range of positions (on a
0-1 scale) over which the middle colour is stretched. Only used when \code{col}
has exactly three colours, \code{scale_method = "ecdf"}, and \code{col_positions = "auto"}. Also applied to diverging \code{palette} presets (those with
\code{col_positions = "auto"}). Default is \code{c(0.4, 0.6)}.}

\item{na_rm}{logical. Whether to remove \code{NA} values before computing
per-cluster statistics. When \code{TRUE} (default), \code{NA} values are removed
and a message is issued showing how many were removed per variable. When
\code{FALSE}, \code{NA} values are passed through: tile fill values will be \code{NA}
(rendered as grey by default) where a variable has no non-missing
observations in a cluster.}

\item{font_size}{numeric. Font size passed to \code{cowplot::theme_cowplot}.
Default is \code{14}.}

\item{thm}{ggplot2 theme object or \code{NULL}. Default is
\code{cowplot::theme_cowplot(font_size = font_size)} with a white plot
background. Set to \code{NULL} to apply no theme adjustment.}

\item{grid}{ggplot2 panel grid or \code{NULL}. Default is
\code{cowplot::background_grid(major = "xy")}. Set to \code{NULL} for no grid.}

\item{show_values}{logical. Whether to overlay the median value for each
cluster-variable combination as a text label on each tile. Default is
\code{FALSE}.}

\item{values_format}{function or \code{NULL}. A function that takes a numeric
vector and returns a character vector of formatted labels. Applied to
the per-cluster median values when \code{show_values = TRUE}. When \code{NULL},
values are formatted to three significant figures without scientific
notation using \code{trimws(format(x, digits = 3, scientific = FALSE))}.
Default is \code{NULL}.}

\item{values_col}{character. Colour for the overlaid text labels.
Default is \code{"black"}.}

\item{values_size}{numeric. Font size (in \code{ggplot2} units) for the
overlaid text labels. Default is \code{3}.}

\item{cluster}{character. Name of the column in \code{.data} that identifies
group membership. Alias for the \code{group} parameter.}

\item{...}{Additional arguments passed to \code{\link[=plot_group_heatmap]{plot_group_heatmap()}}.}
}
\value{
A ggplot object.
}
\description{
Creates a heat map where each tile shows a scaled summary of a variable for
a group. The scaling method is controlled by the \code{scale_method} parameter.
By default (\code{scale_method = "ecdf"}), each tile shows the percentile of the
group's median value compared against the empirical cumulative distribution
function (ECDF) of that variable across all observations not belonging to the
group. Groups and variables are ordered along the axes via hierarchical
clustering.
}
\examples{
set.seed(1)
.data <- data.frame(
  group = rep(paste0("C", 1:3), each = 20),
  var1 = c(rnorm(20, 2), rnorm(20, 0), rnorm(20, -2)),
  var2 = c(rnorm(20, -1), rnorm(20, 1), rnorm(20, 0))
)
plot_group_heatmap(.data, group = "group")
plot_group_heatmap(.data, group = "group", show_values = TRUE)
plot_group_heatmap(.data, group = "group", palette = "alarm")
}
