% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_cluster_density.R
\name{plot_group_density}
\alias{plot_group_density}
\alias{plot_cluster_density}
\title{Plot density of variable values with per-group overlays}
\usage{
plot_group_density(
  .data,
  group,
  vars = NULL,
  col_clusters = NULL,
  palette_group = "auto",
  n_col = NULL,
  n_row = NULL,
  density = "both",
  scale = "max_overall",
  scales = "free_y",
  expand_coord = NULL,
  exclude_min = "no",
  rug = NULL,
  density_overall_weight = NULL,
  bandwidth = "hpi_1",
  na_rm = TRUE,
  alpha = 0.75,
  label = FALSE,
  show_legend = NULL,
  legend = NULL,
  font_size = 14,
  thm = cowplot::theme_cowplot(font_size = font_size) + ggplot2::theme(plot.background =
    ggplot2::element_rect(fill = "white", colour = NA), panel.background =
    ggplot2::element_rect(fill = "white", colour = NA)),
  grid = cowplot::background_grid(major = "xy")
)

plot_cluster_density(.data, cluster, palette_cluster = "auto", ...)
}
\arguments{
\item{.data}{data.frame. Rows are observations. Must contain a column
identifying group membership and columns for variable values.}

\item{group}{character. Name of the column in \code{.data} that identifies
group membership.}

\item{vars}{character vector or \code{NULL}. Names of columns in \code{.data} to
use as variables. If \code{NULL}, all columns except \code{group} are used.
Default is \code{NULL}.}

\item{col_clusters}{named character vector or \code{NULL}. Per-group colours.
Names should match group labels. When \code{NULL} (default), colours are
chosen automatically by \code{palette_group}.}

\item{palette_group}{character. Palette used for automatic colour assignment
when \code{col_clusters} is \code{NULL}. One of \code{"auto"} (default), \code{"okabe_ito"},
\code{"paired"}, \code{"kelly"}, \code{"glasbey"}, or \code{"hue_pal"}. See the \strong{Colour
palette} section of Details.}

\item{n_col}{integer or \code{NULL}. Number of columns passed to
\code{ggplot2::facet_wrap}. If supplied (or if \code{n_row} is supplied) a single
faceted plot is returned instead of a list. Default is \code{NULL}.}

\item{n_row}{integer or \code{NULL}. Number of rows passed to
\code{ggplot2::facet_wrap}. If supplied (or if \code{n_col} is supplied) a single
faceted plot is returned instead of a list. Default is \code{NULL}.}

\item{density}{character. What density to display. One of \code{"both"} (default:
overall density curve plus per-cluster density curves), \code{"overall"}
(overall density curve plus cluster median lines), or \code{"cluster"} (one
density curve per cluster, coloured by cluster). See \strong{Details}.}

\item{scale}{character. How to scale per-cluster density curves. Only
relevant when \code{density} is \code{"cluster"} or \code{"both"}. One of
\code{"max_overall"} (default: each cluster density is rescaled so that its
maximum equals the maximum of the overall density, keeping y-axis values
comparable to the overall density), \code{"max_cluster"} (no rescaling;
y-axis is determined by the tallest curve), or \code{"free"} (no rescaling;
equivalent to \code{"max_cluster"}).}

\item{scales}{character. The \code{scales} argument passed to
\code{ggplot2::facet_wrap} when a faceted plot is requested. Default is
\code{"free_y"} so that the x-axis is shared across panels.}

\item{expand_coord}{numeric vector or named list or \code{NULL}. Expands the
x-axis limits to include the given values. A plain numeric vector is
applied to every variable. A named list (names = variable names, values =
numeric vectors) applies expansion per variable. When a faceted plot is
requested via \code{n_col}/\code{n_row} and a named list is provided, a warning is
issued and \code{expand_coord} is ignored (incompatible with faceting).
Default is \code{NULL}.}

\item{exclude_min}{character. Whether to exclude observations whose value
equals the minimum from the density and median calculations. Options are:
\code{"no"} (default, no exclusion), \code{"overall"} (exclude observations whose
value equals the global minimum across all variables), or \code{"variable"}
(for each variable, exclude observations whose value equals that
variable's minimum).}

\item{rug}{character or \code{NULL}. Controls the rug added below the density.
\code{NULL} (default): per-cluster rug when \code{density} is \code{"cluster"} or
\code{"both"}, overall rug when \code{density} is \code{"overall"}. \code{"cluster"}:
per-cluster rug, coloured by cluster. \code{"overall"}: overall rug with no
cluster colouring. See \strong{Details}.}

\item{density_overall_weight}{character or \code{NULL}. Controls weighting of the
overall density when \code{density} is \code{"overall"} or \code{"both"}. \code{NULL}
(default): the overall density is estimated from all observations pooled
together. \code{"even"}: the overall density is computed as an equal-weight
average of per-cluster kernel densities, preventing larger clusters from
dominating the density estimate. Ignored when \code{density} is \code{"cluster"}.}

\item{bandwidth}{character or positive numeric. Bandwidth used for
per-cluster kernel density estimation. One of \code{"hpi_1"} (default),
\code{"hpi_0"}, \code{"SJ"}, or a positive number. See \strong{Details}.}

\item{na_rm}{logical. Whether to remove \code{NA} values from each variable
before computing densities and medians. When \code{TRUE} (default), \code{NA}
values are removed and a message is issued showing how many were removed
per variable. When \code{FALSE}, \code{NA} values are passed directly to
\code{stats::density()}, which will strip them with its own warning.}

\item{alpha}{numeric. Transparency applied to density curves (both overall
and per-group lines). Must be between 0 (fully transparent) and 1 (fully
opaque). Default is \code{0.75}.}

\item{label}{logical. Whether to add on-plot labels at the highest-density
peak of each group using \code{ggrepel::geom_text_repel}. When \code{density} is
\code{"overall"}, labels are placed at the overall-density value at each
group's median. Default is \code{FALSE}.}

\item{show_legend}{logical or \code{NULL}. Whether to display the legend. \code{NULL}
(default): the legend is shown when the number of groups is 15 or fewer
and hidden otherwise. \code{TRUE}/\code{FALSE}: always show/hide the legend,
overriding the default behaviour. When both \code{show_legend} and the
deprecated \code{legend} are supplied, \code{show_legend} takes precedence.}

\item{legend}{logical or \code{NULL}. Deprecated alias for \code{show_legend}.
Retained for backwards compatibility.}

\item{font_size}{numeric. Font size passed to \code{cowplot::theme_cowplot}.
Default is \code{14}.}

\item{thm}{ggplot2 theme object or \code{NULL}. Default is
\code{cowplot::theme_cowplot(font_size = font_size)} with a white plot
background. Set to \code{NULL} to apply no theme adjustment.}

\item{grid}{ggplot2 panel grid or \code{NULL}. Default is
\code{cowplot::background_grid(major = "xy")}. Set to \code{NULL} for no grid.}

\item{cluster}{character. Name of the column in \code{.data} that identifies
group membership. Alias for the \code{group} parameter.}

\item{palette_cluster}{character. Alias for \code{palette_group} in
\code{\link[=plot_group_density]{plot_group_density()}}. See the \strong{Colour palette} section of Details.}

\item{...}{Additional arguments passed to \code{\link[=plot_group_density]{plot_group_density()}}.}
}
\value{
A named list of ggplot2 objects (one per variable) when neither
\code{n_col} nor \code{n_row} is specified. A single ggplot2 object with
\code{facet_wrap} panels when \code{n_col} or \code{n_row} is specified.
}
\description{
For each variable, plots kernel density estimates with per-group overlays
(density curves and/or median lines). Returns a named list of ggplot2
objects or a single faceted plot.
}
\details{
\subsection{Density modes}{

The \code{density} argument controls what is shown:
\itemize{
\item \code{"both"} (default): the overall density curve plus one density curve per
group, coloured by group. Group curves are scaled according to the
\code{scale} argument.
\item \code{"overall"}: the overall density of all observations with a vertical line
per group at that group's median value.
\item \code{"cluster"}: one density curve per group, coloured by group.
}
}

\subsection{Rug}{

A rug is added by default. The \code{rug} argument controls which data it shows:
\itemize{
\item \code{NULL} (default): per-group rug when \code{density} is \code{"cluster"} or
\code{"both"}, overall rug when \code{density} is \code{"overall"}.
\item \code{"cluster"}: per-group rug, coloured by group.
\item \code{"overall"}: overall rug (no group colouring).
}
}

\subsection{Bandwidth}{

The \code{bandwidth} argument controls per-group bandwidth selection (used for
per-group density curves and for the even-weighted overall density):
\itemize{
\item \code{"hpi_1"} (default): \code{ks::hpi(x, deriv.order = 1)} --- plug-in bandwidth
based on the first derivative, less sensitive to group size than SJ.
\item \code{"hpi_0"}: \code{ks::hpi(x, deriv.order = 0)} --- plug-in bandwidth based on
the density itself.
\item \code{"SJ"}: Sheather-Jones bandwidth via \code{stats::bw.SJ()}.
\item A positive numeric value: use that value as the bandwidth directly.
}

If bandwidth estimation fails, a warning is issued and the default
(\code{stats::bw.nrd0}) bandwidth is used as a fallback.
}

\subsection{Layout}{

By default the function returns a \strong{named list of ggplot2 objects}, one per
variable. If \code{n_col} or \code{n_row} is supplied the plots are instead combined
into a \strong{single faceted ggplot2 object} via \code{facet_wrap}.
}

\subsection{Colour palette}{

When \code{col_clusters} is \code{NULL}, group colours are assigned automatically
based on \code{palette_group}. The \code{"auto"} strategy selects a palette by the
number of groups:
\itemize{
\item \strong{1–8 groups}: Okabe-Ito — colorblind-safe 8-colour palette.
\item \strong{9–12 groups}: ColorBrewer Paired — 12 colours pairing light and dark
versions of 6 hues.
\item \strong{13–21 groups}: Kelly's palette (optional \code{Polychrome} package) — 21
colours of maximum perceptual contrast (white excluded). Falls back to
\code{hue_pal()} with a warning if \code{Polychrome} is not installed.
\item \strong{22–31 groups}: Glasbey's palette (optional \code{Polychrome} package) — 31
algorithmically spaced colours (white excluded). Falls back to \code{hue_pal()}
with a warning if \code{Polychrome} is not installed.
\item \strong{> 31 groups}: \code{hue_pal()} — evenly spaced hues (a warning is issued).
}

Set \code{palette_group} explicitly to override the automatic selection (provided
the chosen palette supports at least as many colours as there are groups).
}
}
\examples{
set.seed(1)
.data <- data.frame(
  group = rep(paste0("C", 1:3), each = 20),
  var1 = c(rnorm(20, 2), rnorm(20, 0), rnorm(20, -2)),
  var2 = c(rnorm(20, -1), rnorm(20, 1), rnorm(20, 0))
)
# Default: overall + per-group density curves
plot_list <- plot_group_density(.data, group = "group")

# Overall density with group median lines only
plot_group_density(.data, group = "group", density = "overall")

# Per-group density curves only
plot_group_density(.data, group = "group", density = "cluster")

# Even-weighted overall density
plot_group_density(
  .data, group = "group", density_overall_weight = "even"
)

# Faceted plot with 2 columns
plot_group_density(.data, group = "group", n_col = 2)

# On-plot labels at density peaks
plot_group_density(.data, group = "group", density = "cluster", label = TRUE)

# Always show the legend regardless of group count
plot_group_density(.data, group = "group", legend = TRUE)
}
